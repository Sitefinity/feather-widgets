<div class="modal-header">
    <button type="button" class="close" ng-click="cancel()">&times;</button>
    <h3 class="modal-title">Image</h3>
</div>

<sf-image-field class="sf-Media--info modal-settings" sf-model="properties.Item.PropertyValue.DataItem.Id" sf-image="properties.Item.PropertyValue" sf-provider="model.provider"></sf-image-field>

<div class="modal-body">
    <form name="imageProperties">
        <div class="form-group" ng-class="{ 'has-error' : !properties.Item.PropertyValue.Fields.Title.Value }">
            <label for="title">Title</label>

            <div class="row">
                <div class="col-xs-8">
                    <input class="form-control" name="title" type="text" ng-model="properties.Item.PropertyValue.Fields.Title.Value" ng-required="true" />
                </div>

                <div class="m-top-xs" ng-class="{'text-danger': properties.Item.PropertyValue.Fields.Title.Value.length > 35}">{{properties.Item.PropertyValue.Fields.Title.Value.length}}</div>
            </div>

            <div class="text-muted">Less than 35 characters are recommended</div>

            <p class="text-danger" ng-show="imageProperties.title.$error.required">Title is required</p>
        </div>
        <div class="form-group">
            <label for="alternativeText">Alternative text</label>
            <div class="row">
                <div class="col-xs-8">
                    <input class="form-control" name="alternativeText" type="text" ng-model="properties.Item.PropertyValue.Fields.AlternativeText.Value" />
                </div>
                <div class="m-top-xs" ng-class="{'text-danger': properties.Item.PropertyValue.Fields.AlternativeText.Value.length>35}">{{properties.Item.PropertyValue.Fields.AlternativeText.Value.length}}</div>
            </div>

            <div class="text-muted">Less than 35 characters are recommended</div>
        </div>

        <div class="form-group">
            <label>Image thumbnail</label>
            <div ng-include="thumbnailSizeTempalteUrl"></div>
        </div>

        <expander expander-title='More options'>
          
            <div class="row">
                <div class="col-xs-9">
                    <label>CSS classes</label>
                    <input class="form-control" type="text" ng-model="properties.CssClass.PropertyValue" />
                </div>
            </div>
        </expander>
    </form>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-primary pull-left" ng-click="done()" ng-disabled="!properties.Item.PropertyValue.Fields.Title.Value">Done</button>
    <button type="button" class="btn btn-link pull-left" ng-click="cancel()">Cancel</button>
</div>
